export type FormValues = {
  email: string;
  password: string;
};
export type SigUpFormValues = {
  firstName: string;
  lastName: string;
  userName: string;
  email: string;
  password: string;
  confirmPassword: string;
};
